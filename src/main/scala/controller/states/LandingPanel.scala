package cl.uchile.dcc.citric
package controller.states

import controller.{GameController, GameState}

class LandingPanelState(controller: GameController) extends GameState(controller) {
  override def doEffect(): Unit = {
    // Lógica para efectuar el efecto del panel
  }
}
